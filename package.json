{
  "name": "rachel8",
  "type": "module",
  "scripts": {
    "start": "bun run src/index.ts",
    "dev": "bun --watch run src/index.ts",
    "setup": "bun run src/setup/wizard.ts",
    "test": "bun test"
  },
  "dependencies": {
    "@clack/prompts": "^1.0.0",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
